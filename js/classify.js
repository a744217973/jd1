$(function() {
    var swiper = new Swiper('.swiper-container', {
        direction: 'vertical',
        slidesPerView: 'auto',
        freeMode: true,
        scrollbar: {
            el: '.swiper-scrollbar',
        },
        mousewheel: true,
    });
})

var JdClassify=function(){

}
JdClassify.prototype ={
	// 左侧边栏分类函数
	classify：function(){
		//获取ul给所有的li标签注册点击事件
		var ul =docuyment.get
	}
};